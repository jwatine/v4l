const tab = 1;
imported node operation_buffer_data(original_buff : char [614400] ) returns (modified_buff : char [614400]) wcet 20;


sensor queue_buff wcet 60;
actuator frame wcet 20;

node camera(queue_buff : char [614400] rate (100,0) ) returns (frame : rate(100,0))
     		       
let
	frame=operation_buffer_data( queue_buff);
tel	
